<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%- locals.pageTitle %></title>
  <meta name="description" content="<%- locals.description %>">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/main.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;700&family=Unbounded:wght@300;700&display=swap" rel="stylesheet">
  <style>

    .new-note{
      background-color: lightgreen;
      color: black;
    }
    .moveRight {
      position: absolute;
      animation: moveToRight 2s forwards; /* Duration: 2 seconds, stays at the final position */
    }

    @keyframes moveToRight {
      from {
        left: 0; /* Initial position */
      }
      to {
        left: 10%; /* Final position (moves to the right) */
      }
    }
    #lyrics{
      display: flex;
      height: 500px;
    }
    .body-truncate {
      max-height: 60px; /* Adjust based on how much content should fit */
      overflow: hidden; /* Hides content that exceeds the max-height */
      text-overflow: ellipsis; /* Adds '...' for overflowed content */
      display: -webkit-box; /* Enables truncating for multiline text */
      -webkit-line-clamp: 3; /* Limits to 3 lines (adjust as needed) */
      -webkit-box-orient: vertical; /* Required for -webkit-line-clamp to work */
    }

  </style>
</head>
<body class="home d-flex flex-column h-100">

  <%- include('../partials/header_dashboard.ejs') %>
  <%- body %>
  <%- include('../partials/footer.ejs') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>
</body>
</html>